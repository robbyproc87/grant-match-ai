Approved for merge with the following notes and one small addition.

Verification packet review — accepted as written:

1. RLS migration verified clean. The asymmetric write posture 
   (service-role only on orgs, org_members, funders, grants, match_scores) 
   is the correct security stance.

2. Scoring code accepted with two intentional upgrades to the brief:
   - Geographic factor's "adjacent fallback" (5/15 for org-has-geographies-
     but-no-overlap) is an improvement over the brief's binary 15/0. Keeping it.
   - Budget factor's ratio-based scoring (1%-50% range = 15, 50-100% = 10, 
     <1% = 8, >100% = 3) is a material upgrade over the brief's "in/close/out" 
     heuristic. Keeping it.
   
   Please add a one-line comment at the top of each of those two factor files 
   noting these are intentional Phase 1 upgrades to the original spec, so 
   future me (or another agent) doesn't "fix" them back.

3. Test (c) verified — assertion is correct, await pattern correctly proves 
   no unhandled throw. Test location at tests/scoring.test.ts is fine.

4. Stuck-computing sweep — your analysis is correct, don't build the timeout 
   heuristic. Defer the proper job table with `SELECT FOR UPDATE SKIP LOCKED` 
   to Phase 2 follow-up #2.

   BUT: I need a Phase 1 defensive escape hatch so a Replit restart 
   doesn't strand users in a permanent skeleton state. Add ONE thing:

   - A button labeled "Recompute all scores" in the Grants tab header 
     (small, secondary styling — not primary)
   - On click, calls a new Server Action `recomputeAllForOrg` that:
     a) Authz-checks org membership server-side (same pattern as 
        recomputeScoresForOrg)
     b) Flips all match_scores rows for that org with status IN ('pending', 
        'computing', 'failed') back to 'pending' with error_message=null
     c) Calls computeOneScore for each
     d) Returns immediately (fire-and-forget, same pattern)
   - Disable the button while polling shows any rows in 'pending' or 
     'computing' to prevent the user from spamming it
   - Add a single test: action requires org membership, flips qualifying 
     rows to pending, no-ops if all rows are 'computed'

5. Test count confirmed at 17. After the recompute-all addition, expect 18.

Once that's in and tests pass, mark Phase 1 complete and let me know.

Two follow-ups officially tracked for Phase 2:
- Generate proper Database TypeScript types via `supabase gen types`
- Durable job queue (replaces fire-and-forget) with SELECT FOR UPDATE 
  SKIP LOCKED worker pattern, attempt counts, and crash recovery