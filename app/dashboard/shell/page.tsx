import { EmptyState } from "@/components/empty-state";

export default function ShellPage() {
  return (
    <EmptyState
      icon="📝"
      title="Application Shell — Coming in Phase 2"
      description="In Phase 2, GrantMatch AI will draft tailored application narratives using your org profile and the selected funder&rsquo;s priorities."
    />
  );
}
